# 便签「透明背景 + 毛玻璃」需求说明（交付给接手 AI）

> 项目：StickyNote（Tauri 2 桌面便签，Rust 后端 + TypeScript/Vite 前端）
> 编写时间：2026-07-29
> 目的：当前「透明背景」模式的毛玻璃效果反复改坏（卡顿/无效/全透明），此处给出**明确需求 + 技术约束 + 失败记录 + 推荐方案**，请接手 AI 据此实现，不要再来回试错。

---

## 一、一句话目标

「透明背景」模式下，便签区域要**像自定义背景图那样对后方内容做毛玻璃模糊**：
- 后方内容 = 便签窗口背后的桌面（不要求是“实时清晰”的桌面，磨砂质感即可）；
- 模糊强度由设置里的「毛玻璃模糊强度」滑块（`glass_blur`，单位 px）控制，拖动要**实时生效**；
- 观感应与「自定义背景图片」模式的毛玻璃**一致**；
- **硬性要求：不能卡、不能让窗口不可用**（上一版把整张 4K 壁纸用 `filter:blur` 磨砂导致卡死，必须避免）。

---

## 二、现有两种背景模式（务必理解）

| 模式 | 设置字段 | 当前实现 | 观感 |
|---|---|---|---|
| 自定义背景图片 | `bg_image` 路径 | `.has-bg` 类 + `::before` 承载图片，`filter:blur` 磨砂图片本身 | 正常、可调控、不卡 |
| 透明背景 | `bg_transparent = true` | `.bg-transparent` 类 + 根元素底色；模糊靠 `backdrop-filter` | **模糊强度滑块不生效**（见第三节） |

两者共用的变量：
- `--glass-blur`：由 `glass_blur`（滑块）写入，单位 px，是模糊半径。
- `--note-glass-bg-alpha / --note-glass-ctrl-alpha / --note-glass-sub-alpha`：由 `applyGlassValues(el, go)` 写入（`go = bg_glass_opacity`，默认 0.3），是底色/控件的不透明度（用于半透明磨砂面板的“雾感”）。
- `glass_enabled`：是否开启毛玻璃（控制 `.glass` 类）。

---

## 三、为什么 CSS `backdrop-filter` 这条路走不通（关键约束）

**本应用是 Tauri 透明窗口（`tauri.conf.json` 里 `"transparent": true`）**。在这种窗口里：
1. 根元素 `.bg-transparent` 上的 `backdrop-filter` **无法采样到“窗口背后的系统桌面”**——Chromium 把透明 webview 当作 backdrop，没有可模糊的真实桌面内容，于是 `backdrop-filter: blur()` 看起来“什么都没发生”，滑块像失效。
2. 把 `backdrop-filter` 放到 `::before` 子层更糟：子层同样采不到桌面，且会让窗口变成“全透明/只剩死色块”。

> 结论：**纯 CSS 无法在 Tauri 透明窗口里稳定“实时磨砂活桌面”**。必须用「图片」或「原生 API」两条路之一。

---

## 四、已经试过、失败的做法（请勿重蹈）

1. ❌ 根元素 `backdrop-filter`（当前基线）：不卡，但**模糊强度无效**（桌面采不到）。
2. ❌ `::before` 子层 `backdrop-filter`：要么全透明、要么死色块。
3. ❌ 透明模式直接读桌面壁纸、当背景图用 `filter:blur` 磨砂：模糊强度滑块生效、观感对了，但**全屏 `filter:blur` 一张 4K 壁纸 + 每次开窗口同步读注册表，导致窗口严重卡顿、几乎不可用**（最新一次，已回退）。
4. ❌ 透明模式底色写死 `rgba(0,0,0,…)`：整片变黑、且主题切换失效（已修为跟随主题 `color-mix(var(--bg) …)`，保留即可）。

---

## 五、推荐方案（二选一，优先 A）

### 方案 A（推荐）：Windows 原生模糊（真·桌面磨砂）
用 Rust 侧 FFI 调用 Windows DWM/Accent API，对便签窗口启用“背后内容模糊”，模糊强度映射到原生参数：
- `DwmEnableBlurBehindWindow` 开模糊；
- 或 `SetWindowCompositionAttribute` + `WCA_ACCENT_POLICY`（AccentState = `ACCENT_ENABLE_BLURBEHIND`）实现亚克力式磨砂；
- 模糊“强度”在原生侧是粗粒度的，可用「半透明主题色 tint（不透明度 = `bg_glass_opacity`）」叠加来提供用户可感的强弱；
- 需要在 `src-tauri/src/main.rs` 里加 unsafe FFI（用 `windows-sys` / `windows` crate），并在窗口创建后调用；仅 Windows 生效，其它平台回退到方案 B 或主题色面板。
- 优点：真正磨砂活桌面、丝滑不卡；缺点：需要 Rust/FFI、跨平台要分支。

### 方案 B（稳妥、零原生代码）：预模糊的壁纸当背景
- 透明模式取桌面壁纸（读取方式见下方“壁纸读取”），**但不要用 `filter:blur` 实时磨砂整张图**；
- 在 Rust 或一次性预处理时把壁纸**缩放+模糊成低位图**（如 1/4 尺寸、blur 一次），缓存为 data URL 当作 `--note-bg-img`，用 `::before background-size:cover` 平铺；
- 滑块改强度时只改 `--glass-blur` 对应的**预模糊半径重算**或只改 tint 不透明度，**绝不对全尺寸原图做 `filter:blur`**；
- 用户已明确“透明效果不一定要清晰展示后面内容”，所以壁纸模糊=磨砂质感完全可接受。
- 优点：纯前端、与图片模式同引擎、观感一致；缺点：不是实时桌面（可接受）。

> 无论 A/B，最终效果都要和「自定义背景图片」模式的毛玻璃观感统一。

---

## 六、需要改动的代码位置

- `src-tauri/src/main.rs`
  - `applyBackground` 对应的 Rust 侧窗口配置 / 新增原生模糊命令（方案 A）；
  - 壁纸读取（方案 B 可用，建议走 Rust 读注册表 `HKCU\Control Panel\Desktop\Wallpaper` 并用 `windows-sys` 读避免中文用户名乱码，不要再用 `reg query` 字符串解析）。
- `src/styles.css`
  - `.note-window.bg-transparent`（根元素底色，保留跟随主题 `color-mix(var(--bg) …)`，不要写死黑）；
  - `.note-window.bg-transparent.glass`（模糊规则，按所选方案替换）；
  - 保留 `z-index` 层级：工具栏 `z-index:3`、编辑区等 `z-index:1`（保证颜色面板可点）。
- `src/note.ts`
  - `applyBackground()` 里 `if (bgTransparent) {…}` 分支（当前在 ~426 行，已回退为纯 `.bg-transparent` 基线）；
  - `applyGlassValues(el, go)`（~381 行，输出 alpha 变量，保留）。
- `src/settings.ts`
  - 透明背景的实时预览（`syncBgTransparent()`，~527 行），需与 note.ts 同步。
- `src/api.ts`
  - 若方案 B 需要新的 Rust 命令，在此加 TS 绑定（当前 `getWallpaper` 已删除，可重新设计）。

---

## 七、验收标准（交付前自测）

1. 选「透明背景」后窗口**可见**（不是全透明、不是全黑），呈半透明磨砂面板/或真实桌面磨砂。
2. 拖动「毛玻璃模糊强度」滑块，**模糊程度实时变化**，且流畅**不卡顿**。
3. 观感与「自定义背景图片」模式的毛玻璃**一致**。
4. 切换浅色/深色主题，透明模式下的面板**跟随主题**（不整片黑）。
5. 背景图 + 毛玻璃下，工具栏的颜色选择面板**能正常点开/点选**（z-index 不被遮挡）。
6. 多便签窗口、主窗口隐藏到托盘再唤出，行为正常。

---

## 八、当前已回退到的可用基线（2026-07-29 17:12 构建）

- 透明模式 = 根元素 `.bg-transparent` 跟随主题底色 + 根 `backdrop-filter`（不卡，但**模糊强度仍不生效**，待接手 AI 按本说明修复）。
- 其余修复（颜色面板层级、工具栏按钮透明化、字号按钮「Aa」、字体「A」可见度、黑洞关闭加速）均已保留、正常工作。
- 构建命令（务必用 PowerShell，Bash 沙箱会因 vite 清 dist 触发批量删除守卫）：
  `cd D:\StickyNote; npm run pack`
